DATABASE_URL=postgresql+psycopg://hruser:hrpassword@host.docker.internal:5432/hrdb?connect_timeout=5     #    postgresql+psycopg://hruser:hrpassword@localhost:5432/hrdb
ALEMBIC_DATABASE_URL=postgresql+psycopg://hruser:hrpassword@host.docker.internal:5432/hrdb?connect_timeout=5    #    postgresql+psycopg://hruser:hrpassword@localhost:5432/hrdb
# локальный режим
RUN_IN_DOCKER=false
DB_HOST=host.docker.internal              #   localhost
DB_PORT=5432
DB_NAME=hrdb
DB_USER=hruser
DB_PASSWORD=hrpassword

STORAGE_LOCAL_ROOT=uploads

# если/когда включим Google Drive:
# GOOGLE_SERVICE_ACCOUNT_FILE=service_account.json

STORAGE_BACKEND=gdrive   # local или gdrive
INBOX_RESUMES=inbox/job_applications
INBOX_VACANCIES=inbox/job_openings

GOOGLE_SERVICE_ACCOUNT_FILE=backend/app/data/ai-hr-assistant-service_account.json   # при работе с gdrive
GOOGLE_DRIVE_FOLDERS_FILE=backend/app/data/gdrive_folders.json
HR_INTERACTIVE_CONFIG=0
OPENAI_API_KEY=sk-proj-...
OPENAI_KEY_PASSPHRASE=@Drew9125
OPENAI_MODEL=gpt-4o-mini
DEBUG=0

# Почта для уведомлений
EMAIL_USER=andrew3763g@gmail.com
EMAIL_PASS=XXXXXXXXXXXXXXXX   # 16 символов, без пробелов
MAIL_SMOKE_RECIPIENT=andrew3763g@gmail.com
# Серверы: SMTP: smtp.gmail.com:465 SSL или :587 STARTTLS; IMAP: imap.gmail.com:993 SSL — для чтения включите IMAP в Gmail
# Gmail → ⚙️ See all settings → Forwarding and POP/IMAP → Enable IMAP → Save.
EMAIL_SMTP_SERVER=smtp.gmail.com
EMAIL_SMTP_PORT=587 # 465 SSL или 587 STARTTLS  
EMAIL_IMAP_SERVER=imap.gmail.com
EMAIL_IMAP_PORT=993 # 993 SSL   
EMAIL_USE_TLS=1  # 1 для 587 порта, 0 для 465
FFMPEG_BIN=ffmpeg
FFPROBE_BIN=ffprobe
EXTRACT_WAV=1            # 1 — извлекать WAV, 0 — отправлять WEBM в STT
MEDIA_TIMESLICE_MS=45000
MEDIA_MAX_CHUNKS=80
MEDIA_UPLOAD_ROOT=uploads
CORS_ALLOW_ORIGINS=*
GD_BACKUPS_FOLDER_ID=     # ID папки Backups в Drive (когда создашь)
ADMIN_TOKEN=changeme      # для админ-эндпоинтов backup/restore
